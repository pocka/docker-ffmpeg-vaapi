language: minimal

script:
  - docker build --tag ffmpeg:${TRAVIS_BRANCH} .
  - docker run --rm -t -tffmpeg:${TRAVIS_BRANCH} -buildconf

deploy:
  on:
    tags: true
  script:
    - echo "Deploying ${TRAVIS_BRANCH}"

